module.exports.function = function weblink (placeUrl) {
  const console = require('console');
  var place=placeUrl;
  // console.log(placeUrl);
  
  let result = '';
  if(placeUrl != undefined){
    result = place;
  }
  var apiaddress="http://place.map.kakao.com/10791909";
    // var place="http://www.naver.com";
    
  return apiaddress 
}
