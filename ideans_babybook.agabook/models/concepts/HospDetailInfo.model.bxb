structure (HospDetailInfo) {
  description (병원주소로 검색한 네이버 검색 결과 주소)

  property (hospDetailLink) {
    type (HospDetailLink)
    min (Optional) max (One)
  }
  property (hospName) {
    type (HospName)
    min (Optional) max (One)
  }

property (hospitalAddress) { 
    type (HospitalAddress)
    min (Optional) max (One)
  }
  property (hospPhone) {
    type (HospitalPhone)
    min (Optional) max (One)
  }
}

